pipeline {
    agent any

    stages {
        stage('build image') {
            steps {
                echo '==========Executing========'
                sh "docker build -t app:latest ."
            }
        }
    }
}